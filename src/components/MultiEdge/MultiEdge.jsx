function MultiEdge({
	edge,
	fill,
	weightMode,
	deleteMode,
	nodes,
	setNodes,
	setEdges,
}) {
	const { from, to, index1, index2, weight, points, type } = edge;
	return <div>MultiEdge</div>;
}

export default MultiEdge;
